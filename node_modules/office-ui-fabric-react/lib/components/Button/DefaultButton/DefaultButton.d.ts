import { BaseButton } from '../BaseButton';
export declare class DefaultButton extends BaseButton {
    protected classNames: {
        base: string;
        variant: string;
        icon: string;
        isDisabled: string;
        isEnabled: string;
        label: string;
        root: string;
    };
    protected onRenderDescription(): any;
}

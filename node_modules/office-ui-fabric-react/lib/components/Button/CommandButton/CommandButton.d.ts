import { BaseButton } from '../BaseButton';
export declare class CommandButton extends BaseButton {
    protected classNames: {
        base: string;
        variant: string;
        icon: string;
        isDisabled: string;
        isEnabled: string;
        label: string;
        root: string;
        flexContainer: string;
    };
    protected onRenderDescription(): any;
}
